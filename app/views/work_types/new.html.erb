<h1>Creation d'un type de travail</h1>
<%= form_for @work_type, :url => { :action => "create" }, :html => {:class => "form_new"} do |f| %>
  
  <% if @work_type.errors.any? %>  
  <div id="errorExplanation">  
    <h2><%= pluralize(@work_type.errors.count, "error") %> prohibited this work type from being saved:</h2>  
    <ul>  
    <% @work_type.errors.full_messages.each do |msg| %>  
      <li><%= msg %></li>  
    <% end %>  
    </ul>  
  </div>  
  <% end %> 

  <%= render :partial => f %>

  <%= f.submit "Create" %>

<% end %>